{
	"name": "kubed-io/penpot_sync-integration",
	"description": "Integration tests (Behat) for penpot_sync. Isolated from the app's root composer so Behat's PHP>=8.2 floor and its deps don't constrain the app itself.",
	"license": "AGPL-3.0-or-later",
	"require-dev": {
		"behat/behat": "^3.32",
		"guzzlehttp/guzzle": "^7.9",
		"phpunit/phpunit": "^12.0"
	},
	"config": {
		"sort-packages": true,
		"allow-plugins": {
			"composer/package-versions-deprecated": true
		}
	},
	"autoload-dev": {
		"psr-4": {
			"OCA\\PenpotSync\\Tests\\Integration\\": "bootstrap/"
		}
	}
}
