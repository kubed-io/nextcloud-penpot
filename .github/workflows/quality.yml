name: 🛡️ Quality

# Slower / advisory quality gates kept out of the fast test loop, split by
# language the same way tests.yml is. Same triggers so every PR into main is
# gated.
#
# PHP job: dependency audit + static analysis (php-cs-fixer + Psalm). CodeQL has
#   no PHP support (PHP is the only top-10 language CodeQL does not analyse), so
#   Psalm IS the PHP code-scanning engine: it emits inline annotations
#   (--output-format=github) AND a SARIF report uploaded to Security → Code
#   scanning.
# JS job:  dependency audit + ESLint. JS code scanning is handled by GitHub's
#   **CodeQL default setup** (enabled in Settings → Code security), which runs
#   its own synthesised workflow and uploads to the same Security tab. Keeping
#   CodeQL out of this workflow avoids a duplicate scan on every PR.
#
# Every step writes a $GITHUB_STEP_SUMMARY block so results are visible on the
# run summary (and, for code scanning, on the PR) whether triggered by a PR or a
# direct push to main.

on:
  pull_request:
    branches:
    - main
  push:
    branches:
    - main

jobs:

  php:
    name: PHP Quality
    runs-on: ubuntu-latest
    permissions:
      contents: read
      # Upload Psalm SARIF to Security → Code scanning (PHP's code-scanning path).
      security-events: write
    steps:
    - name: Checkout
      uses: actions/checkout@v7

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.4'
        coverage: none
        tools: composer:v2

    - name: Install PHP dependencies
      run: composer install --prefer-dist --no-progress

    - name: Audit composer dependencies
      run: |
        echo "## composer audit" >> "$GITHUB_STEP_SUMMARY"
        if composer audit --format=plain | tee audit-composer.txt; then
          echo "✅ No composer advisories" >> "$GITHUB_STEP_SUMMARY"
        else
          status=$?
          { echo '```'; cat audit-composer.txt; echo '```'; } >> "$GITHUB_STEP_SUMMARY"
          exit $status
        fi

    - name: Coding standard (php-cs-fixer)
      run: composer run cs:check

    # The behat suites must partition features/ — a file in no suite would simply
    # never run once the integration workflow selects suites per matrix leg, and
    # every leg would still report green. Pure text check, so it belongs here
    # (seconds) rather than in the integration job (a live Nextcloud + Penpot).
    - name: Behat suites partition features/
      run: bash tests/integration/bin/check-suites.sh

    # Same argument, one step further: a step renamed in a .feature file without
    # its definition is an undefined step, and --strict turns that into a failure
    # MINUTES into a leg that first booted Nextcloud, Postgres, Valkey and two
    # Penpot containers — times seven legs. Nothing about the question needs a
    # running anything, so it is answered here in a second.
    - name: Behat step definitions resolve
      run: bash tests/integration/bin/check-step-definitions.sh

    # The feature files carry no essays; each scenario ends with a one-line
    # pointer into features/AGENTS.md. That split rots in two silent ways — an
    # anchor stops resolving when a scenario is renamed, and prose creeps back
    # into the Gherkin until the spec is unreadable again. Both are text checks.
    - name: Notes breadcrumbs resolve, and the comment budget holds
      run: bash tests/integration/bin/check-notes-anchors.sh

    # One-shot: if the baseline is missing (e.g. after a deliberate reset to
    # re-baseline post-cleanup), generate it from the current state and upload
    # it as an artifact to commit back. No-ops once tests/psalm-baseline.xml
    # exists again.
    - name: Seed Psalm baseline if missing
      id: seed_baseline
      run: |
        if [ ! -f tests/psalm-baseline.xml ]; then
          # --ignore-baseline so the (now-absent) errorBaseline reference in
          # psalm.xml doesn't abort generation.
          vendor/bin/psalm --no-cache --threads=4 --ignore-baseline \
            --set-baseline=tests/psalm-baseline.xml || true
          # Only claim success if a file actually appeared. Announcing
          # seeded=true regardless makes the NEXT step fail on a missing
          # artifact, which reads as an upload problem and buries the real
          # Psalm error one step up.
          if [ -f tests/psalm-baseline.xml ]; then
            echo "seeded=true" >> "$GITHUB_OUTPUT"
          else
            echo "::error::Psalm could not generate tests/psalm-baseline.xml — see the step output above"
            exit 1
          fi
        fi

    - name: Upload regenerated baseline
      if: steps.seed_baseline.outputs.seeded == 'true'
      uses: actions/upload-artifact@v7
      with:
        name: psalm-baseline
        path: tests/psalm-baseline.xml
        if-no-files-found: error

    # Psalm is the PHP code scanner: inline GitHub annotations on new findings
    # AND a SARIF report for the Security tab. The committed psalm-baseline.xml
    # keeps the gate failing only on NEW issues. Always upload the SARIF, even
    # when Psalm exits non-zero, so the Security tab reflects the real state.
    - name: Static analysis (Psalm)
      run: |
        vendor/bin/psalm --no-cache --threads=4 \
          --output-format=github \
          --report=psalm.sarif

    - name: Upload Psalm SARIF to code scanning
      if: always()
      uses: github/codeql-action/upload-sarif@v4
      with:
        sarif_file: psalm.sarif
        category: psalm

    - name: Upload Psalm SARIF artifact
      if: always()
      uses: actions/upload-artifact@v7
      with:
        name: psalm-sarif
        path: psalm.sarif
        if-no-files-found: warn

  js:
    name: JS Quality
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
    - name: Checkout
      uses: actions/checkout@v7

    - name: Setup Node
      uses: actions/setup-node@v6
      with:
        node-version-file: .nvmrc
        cache: npm

    - name: Install JS dependencies
      run: npm ci

    - name: Lint JS (ESLint)
      run: npm run lint

    - name: Audit npm dependencies
      run: |
        echo "## npm audit (production, level=high)" >> "$GITHUB_STEP_SUMMARY"
        if npm audit --omit=dev --audit-level=high 2>&1 | tee audit-npm.txt; then
          echo "✅ No npm advisories at or above high" >> "$GITHUB_STEP_SUMMARY"
        else
          status=$?
          { echo '```'; tail -40 audit-npm.txt; echo '```'; } >> "$GITHUB_STEP_SUMMARY"
          exit $status
        fi

    # JS code scanning lives in GitHub's CodeQL default setup (Settings → Code
    # security → Code scanning). It runs its own workflow and posts a `CodeQL`
    # check on every PR — mark that check as required in branch protection if
    # you want it as a hard gate. No duplicate CodeQL step here.
