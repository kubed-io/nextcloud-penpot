node_modules/
dist/
stuff/

# PHP tooling artifacts (composer / phpunit / php-cs-fixer / psalm)
# NB: tests/psalm-baseline.xml is intentionally committed (the gate needs it),
# once tests/ exists.
vendor/
tests/.phpunit.cache/
tests/results/
.php-cs-fixer.cache
tests/integration/vendor/
tests/integration/junit/

# the csr for the market
.signing
.claude
*.token
