{
  "name": "penpot_sync",
  "version": "0.1.0",
  "description": "Penpot Sync — Nextcloud frontend (Files row icon + Open in Penpot action)",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "build": "vite --mode production build",
    "dev": "vite --mode development build",
    "watch": "vite --mode development build --watch",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "comment": "No src/ yet — this is pre-code scaffolding (see saga/Chapter_1_First_Contact.md). @nextcloud/files (v4 for NC33) + friends land with the Files-row action in a later chapter, mirroring nextcloud-grafana's src/files.js.",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.5.0",
    "globals": "^17.6.0",
    "vite": "^8.0.16",
    "vitest": "^4.1.9"
  },
  "engines": {
    "node": "^20 || ^22 || ^24"
  },
  "dependencies": {
    "@nextcloud/event-bus": "^3.3.0",
    "@nextcloud/files": "^4.0.0",
    "@nextcloud/initial-state": "^2.2.0",
    "@nextcloud/l10n": "^3.2.0",
    "@nextcloud/router": "^3.0.1"
  }
}
